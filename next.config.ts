import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  turbopack: {
    root: "C:/Users/HP/Desktop/Nextjs/ghost-tomato", // points to your project folder
  },
};

export default nextConfig;
